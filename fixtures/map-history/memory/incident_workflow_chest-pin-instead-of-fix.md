---
name: incident-workflow-chest-pin-instead-of-fix
description: "fixture: exists but is superseded, so a still-open verdict resting on it may be stale"
---

SUPERSEDED 2026-07-12 -> merged into incident_workflow_fixture-canonical.

Fixture memory. The citation resolves, but the artifact is retired, so the ledger must warn that a
"still open" verdict resting on it can lag a fix that landed under the merged name.
